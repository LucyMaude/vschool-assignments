//mulitply

function mulitiplication (first, second) {
    var sum = first * second;
    return sum;
    
}

module.exports.multiply = function (first, second) {
    var sum = first * second;
    return sum;   
}